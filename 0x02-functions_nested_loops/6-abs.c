
#include "main.h"
/**
 * _abs - displays the absolute value of given numbers
 * @value: a supposed vslue
 * Return: to return 0 fdor a successful code
 */

int _abs(int value)
{
	if (value < 0)
	{
		return (-(value));
	}
	else
	{
		return  (value);
	}
}
