#include "main.h"

/**
 * print_rev - my function
 * @s: var
 */

void print_rev(char *s)
{
	char *c = s;

	if (s)
	{
		while (*j)
		{
			j++;
		}
		while (s < c--)
		{
			_putchar(*c);
		}
		_putchar('\n')
			_putchar('\n');
	}
}
